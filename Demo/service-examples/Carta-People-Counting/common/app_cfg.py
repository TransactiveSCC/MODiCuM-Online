path = '/demo/app'
video = "video1"  #, "video2", "video3"]  # ,"video2","video3"]

inputs = f"{path}/frames"
outputs = f"{path}/outputs"
model = f"{path}/faster_rcnn_inception/frozen_inference_graph.pb"
